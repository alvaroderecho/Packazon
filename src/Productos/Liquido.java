package Productos;

public class Liquido extends Alimentario{
    public Liquido(int units, double weight, int identifier, boolean secured,String descri, double vol) {
        super(units, weight, identifier, secured, descri, vol);
    }
}
